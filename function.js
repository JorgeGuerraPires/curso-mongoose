const functions = {
    soma: (a, b) => a + b,
    isNull: () => null,
    verificador: (a) => a,
    CriarUsuario: () => {
        return {
            firstName: 'Jorge',
            lastName: 'Pires'
        }
    }
}

module.exports = functions;
